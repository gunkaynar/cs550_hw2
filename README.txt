All programs in the Homework folder were coded and tested in Python 3.10Conda environment was utilized and PyCharm was used as IDE.All programs work in terminal, but for the convenience of the grader, a graphic IDE is recommended because no file output is given. All programs require the datasets in their folder. Please consider using the folders as is.The dependencies are:* Numpy* Pandas* sklearn (Scikit-learn)* matplotlibAll programs are ready to execute. No parameter is needed. Every training, test, calculation and measurements are done by default. 